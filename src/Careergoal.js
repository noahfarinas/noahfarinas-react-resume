export const careerGoal = (props) => {
    return (
      <div className="goal">
        <>
          <h1>{props.workgoal[0]}</h1>
          <p>{props.workgoal[1]}</p>
        </>
      </div>
    );
  };
  
  export default careerGoal;
  